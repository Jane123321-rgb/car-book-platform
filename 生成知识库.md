# 生成知识库

# 一、 变更日志

|**时间**|**版本号**|**变更人**|**变更内容**|
|---|---|---|---|
|2026\.07\.22|V1\.0|许谨怡|初始PRD|

# 二、概述 

## 目标 

车书上传后，在生产知识库菜单可看到上传的车书列表，在该模块，可开始、暂停、取消知识库的生成。

# 三、需求内容

## 列表页：

包含字段有：

- 序号

- 车书名称

- 版本号：不聚合展示，即1\.0和2\.0是两条数据

- 状态：待生成、生成中、已生成

- 生成时间：生成结束的时间，生成结束之前显示\-\-

- 生成按钮：车书上传成功后，出现在该列表，生成按钮可见，暂停与取消按钮不可见

- 暂停按钮：知识库开始生成后，暂停按钮可见

- 继续按钮：暂停后，继续按钮可见

- 取消按钮：知识库开始生成后，取消按钮可见，取消后，生成按钮再次可见

## 详情页

知识库生成之前，详情页展示车书的基本信息\(包括车书名称、ID、版本、简介、附件、创建时间\)；

知识库生成之后，详情页除了车书的基本信息，还要有生成的知识库，初始命名规则为：车书名称\+版本号，要支持重命名。



